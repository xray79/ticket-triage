namespace Notifications.Contracts;

/// <summary>Anchor type so other modules/tests can reference this assembly. Notifications is a
/// pure consumer (email on ticket triage/resolution) — it publishes nothing cross-module yet.</summary>
public static class AssemblyMarker
{
}
