namespace Reporting.Contracts;

/// <summary>Anchor type so other modules/tests can reference this assembly. Reporting is a
/// pure consumer (dashboards read its own read-model) — it publishes nothing cross-module yet.</summary>
public static class AssemblyMarker
{
}
